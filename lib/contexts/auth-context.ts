// Re-export the auth functionality from the main hooks directory
// This maintains compatibility with existing imports while keeping the auth logic centralized
export { useAuth } from "@/hooks/useAuth"